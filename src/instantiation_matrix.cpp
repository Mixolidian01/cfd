// R5: explicit (Flux × Recon × EOS) instantiations have moved to the bottom of
// src/operators.cpp, where all the static helpers they depend on are defined.
// This file is intentionally empty — it remains in the build to document where
// new scheme rows should be added (see "Explicit instantiations" comment there).
